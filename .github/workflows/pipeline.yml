name: Pipeline to Server

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8"]
    steps:
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
